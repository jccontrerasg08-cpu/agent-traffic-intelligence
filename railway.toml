[build]
builder = "RAILPACK"
buildCommand = "pip install ."

[deploy]
startCommand = "PYTHONPATH=src python -m agent_traffic_intelligence.runtime"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
