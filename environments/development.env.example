# Local JSONL development only. Copy outside the repository before assigning a real key.
ATI_HASH_KEY=replace-with-a-local-non-production-key
ATI_MAX_LINE_CHARACTERS=1000000
ATI_MAX_CLIENTS=10000
ATI_MAX_EVENTS_PER_CLIENT=128
ATI_SESSION_WINDOW_SECONDS=900
